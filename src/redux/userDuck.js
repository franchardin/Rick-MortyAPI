// constantes
let initialData = {
    loggedIn: false
}
let LOGIN = "LOGIN";
// reducer
export default function reducer(state = initialData, action){
    switch(action.type){
        default: 
            return state;
    }
}
// actions o action creators